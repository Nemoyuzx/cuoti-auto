# Codex 维护与图片入库协议

本文件是缓存压缩或新会话后的第一入口。不要重新设计已经存在的 OCR、数据库、Web 或 PDF 层；先读本文件和 `docs/ARCHITECTURE.md`，再执行 `./.venv/bin/cuoti doctor`。

## 用户发来错题照片时

1. 用原生图片理解能力逐张查看。多题同页必须拆成多条记录；连续多图属于同一题时可合并理解，但每条记录保留来源页。收录范围取“明显错题/订正题”与“题号被圈出的题”的并集；题号被圈时即使未看到红笔订正也必须入库。
2. 忠实提取题干、选项、学生错误答案、正确答案和批注。所有公式写成 LaTeX：行内 `$...$`，独立公式 `$$...$$`。
3. 科目只能是 `数学`、`英语`、`408`、`政治`。使用下方标准板块；看不清或不能确定时写 `待确认`，不能臆造。
4. 原图没有解析时，补出步骤完整、可独立理解的解析；指出具体错因，给出短知识点标签。
5. 按 `docs/extraction-example.json` 生成临时 JSON，用 Pydantic 校验并导入：

   ```bash
   .venv/bin/cuoti import-json tmp/codex_batch.json --source "/绝对路径/原图.jpg"
   ```

6. 导入后打开或查询新记录，确认 Markdown 已同步。图片哈希和题干联合唯一，重复导入会返回原 ID，不要通过改题干制造副本。
7. 自动识别结果默认 `needs_review: true`；只有逐字、公式、答案和解析都经过核对后才能设为 `false`。
8. 导入展示图片时使用 `normalize_display_image`：先应用 EXIF，再用 Tesseract OSD 校正文字方向；始终保留用户原始图片。复核界面左侧是校正后的派生副本，手动旋转仅用于兜底。

## 标准板块

- 数学：高等数学、线性代数、概率论与数理统计；`chapter` 写更细章节，`section` 写这三个板块之一。
- 英语：阅读理解、完形填空、新题型、翻译、写作、词汇与语法。
- 408：数据结构、计算机组成原理、操作系统、计算机网络。
- 政治：马克思主义基本原理、毛泽东思想和中国特色社会主义理论体系概论、习近平新时代中国特色社会主义思想概论、中国近现代史纲要、思想道德与法治、形势与政策。

## 修改代码时

- SQLite schema 只在 `src/cuoti/db.py` 维护；现有四库结构必须一致。
- OCR/视觉后端只在 `src/cuoti/ingest.py` 增加适配器，不把供应商字段写进核心数据表。
- Web 展示保持紧凑卡片和扁平筛选；PDF 导出任务放在顶部导航栏可点开的浮窗中，用圆环进度与完成对勾反馈，不引入大面积 hero、通用侧边栏或无意义空白。
- 四科错题库使用 `/subject/{slug}` 独立页面，不把科目放回筛选下拉框。PDF 必须通过 `export_jobs.py` 的单线程后台队列导出，页面仅轮询进度，不能恢复为同步等待。
- 完整错题本 PDF 不得包含原始拍照页；纯题版只允许使用人工勾选的不含答案或批注的题图。
- 复核页“删除本题”必须先二次确认并调用 `SubjectStore.delete`：删除前备份 JSON，将无其他题引用的派生图片移入 `backups/deleted_assets`，绝不删除用户原始照片。
- 同时修改 Jinja 模板与路由上下文时，可选变量必须在模板和 `render` 中都有默认值。当前启动器不启用 Python 热重载，但 Jinja 会读取新模板；浏览器验证前要重启服务，避免旧 Python + 新模板的短暂版本错配。
- PDF 修改后必须导出两种版本，用 `pdftoppm` 转成 PNG 并目视检查公式、中文、图片、分页和留白。
- 每次改动运行 `./.venv/bin/pytest`；涉及界面时再做真实浏览器检查。
- 运行时产物只放 `data/inbox`、`tmp`、`output` 或各科 `错题_auto`，不要散落到源码目录。

## 当前架构状态

- 四科独立 SQLite + Markdown：已实现。
- Tesseract / OpenAI Responses / MinerU 适配器：已实现；PaddleOCR 作为后续可选适配器，不重复内嵌模型。
- 聚合筛选网页、详情复核和双 PDF：已实现。
- 下一次继续工作时先看测试结果和 `docs/OPERATIONS.md` 的故障排查，不要从零搭建。
